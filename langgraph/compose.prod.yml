# 生产环境 - 镜像固化模式（代码 COPY 进镜像，无热更新）
# 使用: docker compose -f compose.prod.yml up -d --build
services:
  langgraph:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langgraph
    ports:
      - "8100:8100"
    env_file:
      - ../.env
    environment:
      # 生产模式：禁用热重载
      RELOAD: "false"
      # 显式设置 PostgreSQL 连接（覆盖 .env 中可能的冲突变量）
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ai
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      MINIO_ENDPOINT: minio:9000
      OPENAI_BASE_URL: http://sisyphus:8080/v1
      EMBEDDING_URL: http://embedding:8080/v1
      RERANKER_URL: http://reranker:8080/v1
      DOCLING_URL: http://docling:5001
      CRAWL4AI_URL: http://crawl4ai:11235
      LLM_BASE_URL: http://sisyphus:8080/v1/chat/completions
      OBSIDIAN_URL: https://obsidian:27124
      PG_HOST: postgres
      VAULT_ROOT: /vault
      REPORT_SOURCE_DIR: /data/analysis_reports
    volumes:
      # 生产模式：不挂载代码，仅挂载数据卷
      - /mnt/e/ai-platform/data/obsidian_vault:/vault
      - /mnt/e/ai-platform/data/reports:/data/analysis_reports:ro
    networks:
      - ai-platform
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 4G
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8100/health')\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

networks:
  ai-platform:
    external: true
